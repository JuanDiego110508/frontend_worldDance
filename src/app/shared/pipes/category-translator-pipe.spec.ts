import { CategoryTranslatorPipe } from './category-translator-pipe';

describe('CategoryTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
